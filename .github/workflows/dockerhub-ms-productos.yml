name: Docker Hub - ms-productos

on:
  push:
    branches:
      - develop
    paths:
      - 'ms-productos/**'
  workflow_dispatch:

jobs:
  dockerhub:
    name: Build and Push ms-productos
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v4
        with:
          context: ./ms-productos
          file: ./ms-productos/Dockerfile
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/luis-sanchez-ms-productos:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/luis-sanchez-ms-productos:1.0
